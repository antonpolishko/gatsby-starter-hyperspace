import React from 'react';
import Layout from '../components/layout';

const NotFoundPage = () => (
  <Layout>
    <h1>Страница не найдена</h1>
    <p>Провертье правильность ссылки</p>
  </Layout>
);

export default NotFoundPage;
